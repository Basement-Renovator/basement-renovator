# Based on the "EntityType" enum from Lua. We only include the values that we need.
EntityType = {
    "PICKUP": 5,
    "BALL_AND_CHAIN": 893,
}

# Based on the "PickupVariant" enum from Lua. We only include the values that we need.
PickupVariant = {
    "COLLECTIBLE": 100,
}
